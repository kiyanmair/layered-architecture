package config

// DBPath is the path to the SQLite database file.
const DBPath = "./data/app.db"

// TestDBPath is the path to the SQLite database file used for testing.
const TestDBPath = "../../data/test.db"

// ServerAddress is the address that the server listens on.
const ServerAddress = ":8080"
